import React from 'react';
import {shallow} from 'enzyme';
import Routes from '../components/Routes.jsx';

test('Main renderers At all', () => {
  // Render
  const wrapper = shallow(<Routes  /> );
});